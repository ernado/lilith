// Package mock contains generated mocks for the project's interfaces, produced
// by github.com/matryer/moq. The go:generate directives live next to each
// interface; regenerate with `go generate ./...` (or `make generate`).
package mock
